# 02 - 房间服务器架构

[返回 README](../README.md)

## 1. 服务器职责

- **信令**: 协调客户端连接。
- **状态同步**: 接收客户端操作，广播给其他客户端。
- **持久化**: 定期将房间状态写入 .sf 文件。

## 2. 架构

- WebSocket 长连接。
- 每个房间对应一个会话，会话状态在内存中维护。
- 单节点支持 1000 并发房间。
- 服务器崩溃恢复: 从最近的持久化快照恢复房间状态。
